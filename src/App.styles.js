import {StyleSheet} from "react-native"

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#eceece"
    },
    title: {
        fontSize:40,
        fontWeight: "bold",
        marginLeft: 10,
    },
});